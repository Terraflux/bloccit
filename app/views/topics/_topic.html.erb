<div class="media">
	<div class="media-body">
		<h3><%= link_to topic.name, topic %></h3>
		<small>
			<p><%= topic.description %></p>
			<%= render partial: "labels/list", locals: {labels: topic.labels} %>
			<%= render partial: "ratings/rating", locals: {rating: topic.rating} %>
		</small>
	</div>
</div>