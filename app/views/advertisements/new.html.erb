<h1>New Ad</h1>

<div class="row">
	<div class="col-md-4">
		<p>Guidelines for ads</p>
		<ul>
			<li>Make sure all ad titles are less than 3 words long.</li>
			<li>All copy text must be in iambic pentameter.</li>
			<li>Using prices that are not prime integers will result in bans.</li>
		</ul>
	</div>
	<div class="col-md-8">
		<%= form_for @advertisement do |f| %>
			<div class="form-group">
				<%= f.label :title %>
				<%= f.text_field :title, class: 'form-control', placeholder: "Enter ad title" %>
			</div>
			<div class="form-group">
				<%= f.label :copy %>
				<%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter ad copy" %>
			</div>
			<div class="form-group">
				<%= f.label :price %>
				<%= f.text_area :price, rows: 8, class: 'form-control', placeholder: "Enter ad price" %>
			</div>
			<div class="form-group">
				<%= f.submit "Save", class: 'btn btn-success' %>
			</div>
		<% end %>
	</div>
</div>